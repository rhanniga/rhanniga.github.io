# Build output for the C -> WASM `ask` module. Produced by `make -C engine wasm`
# locally, or by the deploy workflow in CI. Never committed -- clang is
# preinstalled on ubuntu-latest, so rebuilding costs ~2s.
site/ask/*
!site/ask/.gitkeep

# Model weights and conversion intermediates. 87 MB of shards, served from a
# HuggingFace repo in production -- see site/js/llm/config.js. Kept out of git
# because a re-quantization would add another 87 MB to history permanently.
*.hslm
*.bin
!site/data/tokenizer.bin
site/model/

# Python tooling
tools/.venv/
__pycache__/

# Native engine build output
engine/engine
engine/engine-trace
engine/engine-asan
engine/engine-debug
engine/tests/test_matmul
engine/*.o
# Generated by tools/export_tokenizer.py --c-array: 886 KB of decode table for the
# native CLI only. Regenerable, so not committed.
engine/tokenizer_debug.c

.DS_Store
